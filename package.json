{
  "name": "url-shortener-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:client": "pnpm --filer client run dev",
    "dev:api": "pnpm --filer api run dev",
    "build": "pnpm run build",
    "test:api": "pnpm --filter api run test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
